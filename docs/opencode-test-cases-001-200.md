# OpenCode 功能用例 001-200（细化版）

说明：每条用例补充了可执行步骤、自动化命令与关联测试，便于落地执行与追溯。

| ID | 模块 | 用例标题 | 细化执行步骤 | 自动化验证命令 | 关键断言 | 关联测试 |
| --- | --- | --- | --- | --- | --- | --- |
| 001 | CLI | version 显示版本 | 在仓库根目录执行 `tacli version`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/control_test.go:TestReadPlanFileReadsRootPlan` |
| 002 | CLI | ping 健康检查 | 在仓库根目录执行 `tacli ping`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/control_test.go:TestReadPlanFileReportsMissingRootPlan` |
| 003 | CLI | models 输出模型列表 | 在仓库根目录执行 `tacli models`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/control_test.go:TestRunPlanPrintsWorkspacePlan` |
| 004 | CLI | status 基础状态 | 在仓库根目录执行 `tacli status`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/control_test.go:TestRunStatusPrintsWorkspaceState` |
| 005 | CLI | plan 读取存在文件 | 在仓库根目录执行 `tacli plan`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/control_test.go:TestRunCapabilitiesListsPacks` |
| 006 | CLI | plan 缺失文件提示 | 在仓库根目录执行 `tacli plan`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/control_test.go:TestRunSkillsListsWorkspaceSkill` |
| 007 | CLI | contract 读取任务契约 | 在仓库根目录执行 `tacli contract`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestPrintUsageIncludesVersion` |
| 008 | CLI | init 初始化脚手架 | 在仓库根目录执行 `tacli init`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestBuildConversationSummaryInputKeepsRecentMessages` |
| 009 | CLI | run 单次任务执行 | 在仓库根目录执行 `tacli run "echo smoke"`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestBuildConversationSummaryInputTruncatesLargeEntries` |
| 010 | CLI | run 缺失参数报错 | 在仓库根目录执行 `tacli run "echo smoke"`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestPeelGlobalDangerously` |
| 011 | CLI | chat 启动交互模式 | 在仓库根目录执行 `tacli status`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestWithDangerouslyFlag` |
| 012 | CLI | help 输出命令帮助 | 在仓库根目录执行 `tacli help`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestRemoteOwnerFromURLParsesGitHubURL` |
| 013 | CLI | dangerously 模式切换 | 在仓库根目录执行 `tacli status`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestPolicyCommandUpdatesAndPersistsStore` |
| 014 | CLI | resume 参数生效 | 在仓库根目录执行 `tacli status`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestPolicyCommandManagesCommandRules` |
| 015 | CLI | 中文语言提示 | 在仓库根目录执行 `tacli status`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestCapabilitiesCommandShowsNamedPack` |
| 016 | CLI | 未知命令返回错误 | 在仓库根目录执行 `tacli status`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestStartupMode` |
| 017 | CLI | 默认 output 模式 | 在仓库根目录执行 `tacli status`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestParseAgentFlagsValidatesOutputMode` |
| 018 | CLI | control status 子命令 | 在仓库根目录执行 `tacli status`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestParseAgentFlagsRejectsStructuredChatOutput` |
| 019 | CLI | control contract 子命令 | 在仓库根目录执行 `tacli contract`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestParseAgentFlagsStateDirFollowsWorkDirByDefault` |
| 020 | CLI | control skills 子命令 | 在仓库根目录执行 `tacli status`，记录 stdout/stderr 与退出码 | go test ./cmd/tacli -count=1 | 输出与退出码符合命令契约，参数/模式解析正确 | `cmd/tacli/main_test.go:TestParseAgentFlagsPreservesExplicitStateDir` |
| 021 | Session | new 创建新会话 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestResolveChatSessionName` |
| 022 | Session | new 重名冲突提示 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestSummarizeJobForSession` |
| 023 | Session | resume 恢复已有会话 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestSavePersistsSessionState` |
| 024 | Session | resume 不存在会话提示 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestSavedSessionStateCanResumeToolWorkflow` |
| 025 | Session | rename 重命名成功 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestResumeCommandRestoresSavedMemory` |
| 026 | Session | rename 目标重名冲突 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestResumeConversationRestoresPlanningState` |
| 027 | Session | fork 生成分支会话 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestRenameAndForkConversationCommands` |
| 028 | Session | fork 自动命名 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestTreeCommandShowsConversationHierarchy` |
| 029 | Session | fork 保留历史消息 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestResetClearsPlanningState` |
| 030 | Session | fork 写入 parent_session | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestRenderSessionRecordIncludesMessagesAndToolCalls` |
| 031 | Session | status 显示 session_id | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestSaveCommandWritesExportFile` |
| 032 | Session | tree 展示会话树 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/parity_test.go:TestCLIParitySessionResumeScenario` |
| 033 | Session | tree 标记 current 节点 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/chat_tui_test.go:TestSessionLoadReloadsVisibleConversation` |
| 034 | Session | save 导出会话记录 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `internal/session/store_test.go:TestSaveLoadRoundTrip` |
| 035 | Session | reset 清空上下文 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `internal/session/store_test.go:TestListSessionsSortedBySavedAt` |
| 036 | Session | resume 恢复 todo 列表 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `internal/session/store_test.go:TestBuildSessionTreeResolvesIDAndLegacyPathParents` |
| 037 | Session | resume 恢复 task contract | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `internal/session/store_test.go:TestNewSessionIDHasStablePrefix` |
| 038 | Session | resume 恢复 memory 作用域数据 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestResolveChatSessionName` |
| 039 | Session | 会话转录文件写入 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestSummarizeJobForSession` |
| 040 | Session | 会话 trace 文件写入 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestSavePersistsSessionState` |
| 041 | Session | 会话 compact 后可继续 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestSavedSessionStateCanResumeToolWorkflow` |
| 042 | Session | 多次 rename 链路保持 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestResumeCommandRestoresSavedMemory` |
| 043 | Session | fork 后继续对话可保存 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestResumeConversationRestoresPlanningState` |
| 044 | Session | resume 后 approval mode 恢复 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestRenameAndForkConversationCommands` |
| 045 | Session | resume 后 model 恢复 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestTreeCommandShowsConversationHierarchy` |
| 046 | Session | 会话时间排序显示 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestResetClearsPlanningState` |
| 047 | Session | 会话消息计数正确 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestRenderSessionRecordIncludesMessagesAndToolCalls` |
| 048 | Session | 树结构兼容 legacy parent path | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/main_test.go:TestSaveCommandWritesExportFile` |
| 049 | Session | 树结构兼容 parent id | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/parity_test.go:TestCLIParitySessionResumeScenario` |
| 050 | Session | new 后 session_id 重新生成 | 启动 `tacli chat --dangerously`，执行 `/new` `/resume` `/fork` `/tree` `/save` `/reset` 等对应命令并检查 `.tacli/sessions` 与转录文件 | go test ./cmd/tacli ./internal/session -count=1 | 会话状态可持久化并可恢复，分支/树结构正确 | `cmd/tacli/chat_tui_test.go:TestSessionLoadReloadsVisibleConversation` |
| 051 | Queue | busy 时输入进入 steering 队列 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestPruneConversationKeepsLatestUserTurnWithToolResult` |
| 052 | Queue | /steer 入队成功 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestCompactConversationAggressiveToolCompactionKeepsTailFacts` |
| 053 | Queue | /steer 空消息拒绝 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestCompactConversationSummaryCarriesOlderToolFacts` |
| 054 | Queue | /follow 入队成功 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestExtractStructuredToolFactsFindsKeyValueFacts` |
| 055 | Queue | /follow 空消息拒绝 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestTruncateToolMessageShrinksLargeOutput` |
| 056 | Queue | steering 在下一轮请求前注入 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestRunTaskStopsRepeatedToolLoopWithoutStepBudget` |
| 057 | Queue | follow-up 在完成后续跑 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestAnnotateToolResultAppendsReminder` |
| 058 | Queue | follow-up 保持工具可用 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestShouldRetryEmptyToolAnswerAfterToolResult` |
| 059 | Queue | 多条 steering 顺序保持 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestShouldNotRetryEmptyToolAnswerTwice` |
| 060 | Queue | 多条 follow-up 顺序保持 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestShouldRetryEmptyToolAnswerAgainForNewToolBlock` |
| 061 | Queue | steering 与 follow-up 混合排队 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestShouldRetryFailedCommandAfterEmptyAnswer` |
| 062 | Queue | 队列消息写入会话历史 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestShouldNotRetryFailedCommandTwice` |
| 063 | Queue | 工具回合结束后继续执行 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestShouldRetryFailedCommandAgainForNewToolBlock` |
| 064 | Queue | finish gate 阻断空回答 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestDecideTurnExecutesToolCalls` |
| 065 | Queue | post-tool answer reminder 触发 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestDecideTurnRetriesOnEmptyPostToolAnswer` |
| 066 | Queue | 命令失败重试提醒触发 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestDecideTurnUsesToolOutputAfterRepeatedEmptyAnswer` |
| 067 | Queue | 计划门控提醒触发 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestEvidenceRetryReminderRequestsDirectFileRead` |
| 068 | Queue | 重复失败 mutating 冷却提醒 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestEvidenceRetryReminderRequestsOfficialURL` |
| 069 | Queue | 流式输出与队列并存 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestEvidenceRetryReminderSkipsWhenDirectReadAlreadyUsed` |
| 070 | Queue | interrupt 时前台任务可中断 | 在 chat 中触发至少一轮工具调用；任务运行期间发送 `/steer ...` 或 `/follow ...`，观察后续回合消息顺序与最终输出 | go test ./internal/agent -count=1 | steering/follow-up 注入时机、顺序与回合续跑正确 | `internal/agent/agent_test.go:TestDecideTurnFinishesWithFinalText` |
| 071 | Policy | approval prompt 模式询问 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/role_policy_test.go:TestEvaluateRolePermissionVerifyDeniesWriteFile` |
| 072 | Policy | approval allow 模式直通 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/role_policy_test.go:TestEvaluateRolePermissionVerifyAllowsReadOnlyRunCommand` |
| 073 | Policy | approval read-only 拒绝写操作 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/role_policy_test.go:TestEvaluateRolePermissionVerifyDeniesMutatingRunCommand` |
| 074 | Policy | approval workspace-write 拒绝越界写 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/role_policy_test.go:TestEvaluateRolePermissionPlanDeniesEditFile` |
| 075 | Policy | policy default allow 生效 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/runtime_test.go:TestHookRunnerAllowsExitCodeZeroAndCapturesStdout` |
| 076 | Policy | policy default deny 生效 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/runtime_test.go:TestHookRunnerDeniesExitCodeTwo` |
| 077 | Policy | policy tool 粒度覆盖 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/runtime_test.go:TestApprovalPermissionDeciderChecksEditFileWrites` |
| 078 | Policy | policy command rule 新增 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/runtime_test.go:TestApprovalPermissionPolicyReturnsStructuredDecision` |
| 079 | Policy | policy command rule 删除 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/runtime_test.go:TestApprovalPermissionPolicyReadOnlyDeniesWrites` |
| 080 | Policy | policy 持久化后重启可恢复 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/runtime_test.go:TestApprovalPermissionPolicyWorkspaceWritePromptsCommands` |
| 081 | Policy | 拒绝后错误信息清晰 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/runtime_test.go:TestApprovalPermissionPolicyAllowsCommandRuleOverride` |
| 082 | Policy | 批准后命令继续 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/runtime_test.go:TestApprovalPermissionPolicyDeniesCommandRuleOverride` |
| 083 | Policy | 文件写审批弹窗提示 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/registry_pipeline_test.go:TestRegistryCallPipelineAllowsHookMutation` |
| 084 | Policy | 命令审批弹窗提示 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/registry_pipeline_test.go:TestRegistryCallPermissionDeniedAudited` |
| 085 | Policy | 非 TTY 审批报错 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/registry_pipeline_test.go:TestRegistryCallStructuredForRuntimeSkipsPermissionAndHookRunner` |
| 086 | Policy | danger-full-access 放行 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/files_test.go:TestWriteFileToolRejectsWhenApproverDenies` |
| 087 | Policy | policy 列表输出完整 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/files_test.go:TestWriteFileToolPreservesExistingPermissions` |
| 088 | Policy | policy 非法参数校验 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/background_test.go:TestDelegateSubagentToolStartsRoleTask` |
| 089 | Policy | command_rules 计数正确 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/edit_test.go:TestEditFileToolPreservesExistingPermissions` |
| 090 | Policy | audit 记录审批决策 | 在 chat 中执行 `/approval ...` 与 `/policy ...`，再触发写文件/命令工具，验证提示、放行与拒绝路径 | go test ./internal/tools -run 'Approval\|Permission\|Policy\|Role' -count=1 | 审批/策略判定与命令规则覆盖符合预期 | `internal/tools/permissions_test.go:TestPermissionStoreReplace` |
| 091 | Files | read_file 读取文本文件 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/role_policy_test.go:TestEvaluateRolePermissionVerifyDeniesWriteFile` |
| 092 | Files | read_file 读取大文件截断 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/role_policy_test.go:TestEvaluateRolePermissionPlanDeniesEditFile` |
| 093 | Files | write_file 新建文件 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/runtime_test.go:TestValidateInputSchemaRequired` |
| 094 | Files | write_file 覆盖文件 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/runtime_test.go:TestValidateInputSchemaTypeMismatch` |
| 095 | Files | edit 局部替换成功 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/runtime_test.go:TestValidateToolInputWithSchema` |
| 096 | Files | edit 上下文不匹配报错 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/runtime_test.go:TestApprovalPermissionDeciderChecksEditFileWrites` |
| 097 | Files | find 文本匹配 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/review_test.go:TestReviewDiff` |
| 098 | Files | find 空结果 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/registry_pipeline_test.go:TestRegistryCallPipelineSuccess` |
| 099 | Files | glob 匹配文件 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/registry_pipeline_test.go:TestRegistryCallPipelineAllowsHookMutation` |
| 100 | Files | glob 非法 pattern 报错 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/registry_pipeline_test.go:TestRegistryCallPipelineFailure` |
| 101 | Files | ls 列目录 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/registry_pipeline_test.go:TestRegistryCallPipelineCanHandleToolError` |
| 102 | Files | 路径归一化处理 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/files_test.go:TestWriteFileToolRejectsWhenApproverDenies` |
| 103 | Files | 禁止越界读取 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/files_test.go:TestWriteFileToolWritesWhenApproved` |
| 104 | Files | 禁止越界写入 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/files_test.go:TestWriteFileToolPreservesExistingPermissions` |
| 105 | Files | 二进制文件读取提示 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/files_test.go:TestReadFileToolRejectsBinaryContent` |
| 106 | Files | UTF-8 文件读写 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/files_test.go:TestReadFileToolTruncatesLargeText` |
| 107 | Files | CRLF 文件兼容 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/edit_test.go:TestEditFileToolReplacesExactBlock` |
| 108 | Files | 多文件批量读取 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/edit_test.go:TestEditFileToolRejectsMissingOldText` |
| 109 | Files | 权限不足错误透传 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/edit_test.go:TestEditFileToolRejectsAmbiguousMatch` |
| 110 | Files | 软链接处理策略 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/edit_test.go:TestEditFileToolPreservesExistingPermissions` |
| 111 | Files | 空文件读取 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/role_policy_test.go:TestEvaluateRolePermissionVerifyDeniesWriteFile` |
| 112 | Files | 不存在文件报错 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/role_policy_test.go:TestEvaluateRolePermissionPlanDeniesEditFile` |
| 113 | Files | 目录当文件读取报错 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/runtime_test.go:TestValidateInputSchemaRequired` |
| 114 | Files | 写入后再次读取一致 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/runtime_test.go:TestValidateInputSchemaTypeMismatch` |
| 115 | Files | audit 记录文件工具参数 | 通过 chat 触发 `read_file`/`write_file`/`edit_file`/`find`/`ls`，检查文件内容变化与越界拦截 | go test ./internal/tools -run 'ReadFile\|WriteFile\|EditFile\|RegistryCallPipeline\|ValidateToolInput\|ReviewDiff' -count=1 | 文件读取/编辑/写入行为正确且安全边界有效 | `internal/tools/runtime_test.go:TestValidateToolInputWithSchema` |
| 116 | Shell | run_command 执行 echo | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestValidateCommandBlocksDangerousRMVariants` |
| 117 | Shell | run_command 默认超时 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestValidateCommandAllowsSafeRM` |
| 118 | Shell | run_command 自定义超时 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolUsesDefaultTimeoutWhenUnset` |
| 119 | Shell | run_command 注入 HOME/XDG_CACHE_HOME/GOCACHE | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolHonorsTimeoutOverride` |
| 120 | Shell | run_command 保留显式缓存变量 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestConfigureCommandCancellationSetsCancel` |
| 121 | Shell | 危险 rm -rf / 阻断 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolInjectsDefaultCacheEnvWhenMissing` |
| 122 | Shell | 危险关机命令阻断 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolPreservesExplicitCacheEnv` |
| 123 | Shell | 长驻前台进程阻断 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolRejectsForegroundLongRunningCommands` |
| 124 | Shell | 裸后台长服务阻断 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolRejectsChainedForegroundHttpServer` |
| 125 | Shell | nohup 后台长服务允许 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolRejectsBareBackgroundingForLongRunningServices` |
| 126 | Shell | 命令无输出返回 no output | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolAllowsDetachedBackgroundingForLongRunningCommands` |
| 127 | Shell | 命令失败返回 command failed | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolPersistsLargeOutputToLogFile` |
| 128 | Shell | 大输出截断并落盘 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolCallStreamEmitsUpdates` |
| 129 | Shell | CallStream 输出增量更新 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestValidateCommandBlocksDangerousRMVariants` |
| 130 | Shell | stdout/stderr 合并输出 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestValidateCommandAllowsSafeRM` |
| 131 | Shell | pipe 关闭错误可忽略 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolUsesDefaultTimeoutWhenUnset` |
| 132 | Shell | shell 参数校验 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolHonorsTimeoutOverride` |
| 133 | Shell | workdir 生效 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestConfigureCommandCancellationSetsCancel` |
| 134 | Shell | 审计记录命令调用 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolInjectsDefaultCacheEnvWhenMissing` |
| 135 | Shell | 并发 shell 调用隔离 | 通过 chat 触发 `run_command`（普通/超时/长输出/危险命令），检查输出、错误与日志落盘 | go test ./internal/tools -run 'RunCommandTool\|ValidateCommand\|ConfigureCommandCancellation' -count=1 | 命令执行、超时、安全拦截与流式输出符合预期 | `internal/tools/shell_test.go:TestRunCommandToolPreservesExplicitCacheEnv` |
| 136 | WebMCP | fetch_url 成功抓取 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/mcp_test.go:TestListMCPResourcesToolUsesConfiguredServer` |
| 137 | WebMCP | fetch_url 返回 fetched_at | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/mcp_test.go:TestReadMCPResourceToolFormatsTextContent` |
| 138 | WebMCP | fetch_url 返回 content_bytes | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestParseDDGResultsFallbackAnchors` |
| 139 | WebMCP | fetch_url HTTP 错误含 body snippet | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestWebSearchUsesSecondaryEndpointFallback` |
| 140 | WebMCP | web_search 返回结果列表 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestWebSearchPrefersGitHubRepositorySearchForRepoQueries` |
| 141 | WebMCP | web_search 去重 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestRerankSearchResultsPrefersGitHubForGitHubQuery` |
| 142 | WebMCP | web_search 标注 source endpoint | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestSearchQueryVariantsPreferExactGitHubRepoSearch` |
| 143 | WebMCP | web_search 空查询校验 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestFetchURLRejectsUnsupportedScheme` |
| 144 | WebMCP | MCP add server | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestFetchURLReturnsErrorForHTTPFailure` |
| 145 | WebMCP | MCP remove server | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/mcp/client_test.go:TestReadFrameParsesContentLength` |
| 146 | WebMCP | MCP list server | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/mcp/client_test.go:TestSameJSONRPCID` |
| 147 | WebMCP | MCP server 参数持久化 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/mcp/client_test.go:TestWriteFrameIncludesContentLengthHeader` |
| 148 | WebMCP | MCP 资源列表读取 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/mcp_test.go:TestListMCPResourcesToolUsesConfiguredServer` |
| 149 | WebMCP | MCP resource template 列表 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/mcp_test.go:TestReadMCPResourceToolFormatsTextContent` |
| 150 | WebMCP | MCP read resource | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestParseDDGResultsFallbackAnchors` |
| 151 | WebMCP | MCP 调用异常透传 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestWebSearchUsesSecondaryEndpointFallback` |
| 152 | WebMCP | MCP 超时处理 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestWebSearchPrefersGitHubRepositorySearchForRepoQueries` |
| 153 | WebMCP | web 工具审计日志 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestRerankSearchResultsPrefersGitHubForGitHubQuery` |
| 154 | WebMCP | fetch_url 非法 URL 报错 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestSearchQueryVariantsPreferExactGitHubRepoSearch` |
| 155 | WebMCP | web/mcp 与会话状态共存 | 通过 chat 触发 `fetch_url`/`web_search`/MCP 相关工具，检查成功返回与异常分支 | go test ./internal/tools ./internal/mcp -run 'Web\|FetchURL\|MCP\|ReadFrame\|WriteFrame' -count=1 | Web/MCP 请求、错误处理与结果格式正确 | `internal/tools/web_test.go:TestFetchURLRejectsUnsupportedScheme` |
| 156 | MemoryTask | memory remember project | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestExtractStableMemoryNotes` |
| 157 | MemoryTask | memory remember global | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestExtractStableMemoryNotesSkipsTransientRequests` |
| 158 | MemoryTask | memory team remember | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestTeamMemoryCommandRememberAndForget` |
| 159 | MemoryTask | memory forget project | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestReadNonInteractiveTasksTreatsMultilineInputAsSingleTask` |
| 160 | MemoryTask | memory forget global | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestReadNonInteractiveTasksSupportsNulSeparatedBatches` |
| 161 | MemoryTask | memory team forget | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestResumeCommandRestoresSavedMemory` |
| 162 | MemoryTask | memory show 汇总 | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestParseNaturalLanguageMemoryIntentRemember` |
| 163 | MemoryTask | memory scope key 正确 | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestParseNaturalLanguageMemoryIntentShow` |
| 164 | MemoryTask | memorize 自动提炼 | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestExecuteTaskNaturalLanguageRemember` |
| 165 | MemoryTask | tasks add | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestExecuteTaskNaturalLanguageForgetLast` |
| 166 | MemoryTask | tasks list | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestExecuteTaskNaturalLanguageShowMemory` |
| 167 | MemoryTask | tasks update status | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestBuildAutoExploreTask` |
| 168 | MemoryTask | tasks remove | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestAdvanceTodoWithJobSummaryProgressesPlan` |
| 169 | MemoryTask | todo update tool 生效 | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestAdvanceTodoWithJobSummarySeedsPlanWhenEmpty` |
| 170 | MemoryTask | todo 替换策略 | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestBuildAutoExploreTaskRequiresStructuredSections` |
| 171 | MemoryTask | contract update tool 生效 | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestBeforeExitCtrlCStyleSkipsAutoMemorySummarization` |
| 172 | MemoryTask | contract 展示命令 | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestBeforeExitAutoMemoryFallsBackWithoutProviderError` |
| 173 | MemoryTask | resume 恢复 tasks | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestBeforeExitAutoMemorySilentlySkipsProviderErrorWithoutFallback` |
| 174 | MemoryTask | task contract 持久化 | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestInterruptCommandCancelsForegroundTask` |
| 175 | MemoryTask | memory 与 prompt 注入一致 | 在 chat 中执行 `/memory` `/remember` `/forget` `/tasks` `/contract`，检查状态文件与恢复行为 | go test ./cmd/tacli ./internal/memory ./internal/tasks ./internal/tools -run 'Memory\|Task\|Todo\|Contract\|Remember\|Forget\|Memorize' -count=1 | 记忆、任务、todo、contract 的状态一致且可恢复 | `cmd/tacli/main_test.go:TestInterruptCommandReportsIdleWhenNoForegroundTask` |
| 176 | BGPlugin | bg 启动后台任务 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/main_test.go:TestCapabilitiesCommandShowsNamedPack` |
| 177 | BGPlugin | bg-role 路由 explore | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/main_test.go:TestBgRoleUsage` |
| 178 | BGPlugin | bg-role 路由 implement | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/main_test.go:TestAgentsCommandListsSnapshots` |
| 179 | BGPlugin | jobs 列表输出 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/main_test.go:TestReloadPluginsCommandWithoutLoadedPlugins` |
| 180 | BGPlugin | job 查看详情 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestSummarizeBackgroundResultStructured` |
| 181 | BGPlugin | job-send 追加指令 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestSummarizeBackgroundResultFallback` |
| 182 | BGPlugin | job-cancel 取消任务 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestRenderJobSummary` |
| 183 | BGPlugin | job-apply 注入对话 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestCollectReadyForApply` |
| 184 | BGPlugin | agents 命令输出 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestValidateBackgroundRole` |
| 185 | BGPlugin | hooks 命令输出 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestFormatJobListIncludesRole` |
| 186 | BGPlugin | plugin list | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestRouteBackgroundRole` |
| 187 | BGPlugin | plugin load | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestShouldPairBackgroundVerify` |
| 188 | BGPlugin | reload-plugins 生效 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestBuildVerifyFollowupTask` |
| 189 | BGPlugin | skills 列表输出 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `cmd/tacli/jobs_test.go:TestBuildBackgroundTaskContract` |
| 190 | BGPlugin | capabilities 列表输出 | 在 chat 中执行 `/bg` `/bg-role` `/jobs` `/job-send` `/plugin` `/skills`，验证后台任务与插件链路 | go test ./cmd/tacli ./internal/tools ./internal/plugins -run 'Bg\|Background\|Plugin\|Skill\|Jobs\|Agents\|Capabilities' -count=1 | 后台任务、插件、技能链路可用且结果可回灌 | `internal/plugins/plugins_test.go:TestDiscoverDirFindsSharedObjects` |
| 191 | Observe | audit stats 输出 | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `cmd/tacli/main_test.go:TestAuditCommandStatsAndTail` |
| 192 | Observe | audit tail 输出 | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `cmd/tacli/main_test.go:TestDebugToolCallCommand` |
| 193 | Observe | audit errors 过滤 | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `cmd/tacli/main_test.go:TestDebugToolCallTailAndErrors` |
| 194 | Observe | debug-tool-call latest | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `cmd/tacli/main_test.go:TestDebugToolCallReplay` |
| 195 | Observe | debug-tool-call tail | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `cmd/tacli/main_test.go:TestDebugTurnCommand` |
| 196 | Observe | debug-tool-call replay | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `cmd/tacli/main_test.go:TestRenderSessionRecordIncludesMessagesAndToolCalls` |
| 197 | Observe | debug-turn latest | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `internal/transport/structured_test.go:TestNormalizeOutputMode` |
| 198 | Observe | debug-turn tail | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `internal/transport/structured_test.go:TestWriterEmitsJSONLines` |
| 199 | Observe | trace tail 输出 | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `internal/transport/structured_test.go:TestAgentEventSinkWritesStructuredAgentPayloads` |
| 200 | Observe | runtime 事件落 trace | 执行 `/audit` `/debug-tool-call` `/debug-turn` `/trace`，校验输出内容与对应日志文件 | go test ./cmd/tacli ./internal/tools ./internal/transport -run 'Audit\|Debug\|Trace\|Record\|Structured' -count=1 | 审计、调试、追踪数据可见且内容完整 | `internal/tools/audit_test.go:TestFileAuditSinkWritesJSONL` |
